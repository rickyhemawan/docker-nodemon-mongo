# Docker, Mongo, Node, Nodemon Setup

execute to run

```
docker-compose up -d --build
```

execute to stop & delete containers

```
docker-compose down
```

optionally for fresh start

```
docker-compose down --rmi all
```